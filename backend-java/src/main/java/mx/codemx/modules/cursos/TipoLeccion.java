package mx.codemx.modules.cursos;

public enum TipoLeccion {
    /** Lección que enseña un concepto (cómo se hace en Python). */
    TEORIA,
    /** Lección práctica que enlaza a un reto que el alumno debe resolver. */
    EJERCICIO
}
